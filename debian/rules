#!/usr/bin/make -f

include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/ruby-pkg-tools/1/class/ruby-setup-rb.mk


# Add here any variable or target overrides you need.

# install/libmongo-ruby1.8::
# 	rm -rf debian/tmp
# 	mv debian/$(cdbs_curpkg) debian/tmp

# install/libbson-ruby1.8::
# 	rm -rf debian/tmp
# 	mv debian/$(cdbs_curpkg) debian/tmp

# install/libcbson-ruby1.8::
# 	rm -rf debian/tmp
# 	mv debian/$(cdbs_curpkg) debian/tmp
